Executable files in this directory, are executed before sync.

INCLUDE_FILE and EXCLUDE_FILE, are the files that contain the list of files to include and exclude. Those two variables are passed as arguments to the scripts of this directory when they are executed.

If you whant to add files to sync, append them to the INCLUDE_FILE. Append them to EXCLUDE_FILE to remove files from sync
