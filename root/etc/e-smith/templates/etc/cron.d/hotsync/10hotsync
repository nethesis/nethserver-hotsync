{
    if ( $hotsync{role} ne 'spare' && $hotsync{RemoteHost} ne '') 
    {
        $OUT='0,15,30,45 * * * * root /usr/sbin/hotsync > /dev/null';
    } else {
        $OUT="#HotSync disabled in spare server\n#0,15,30,45 * * * * root /usr/sbin/hotsync > /dev/null";
    }
} 
