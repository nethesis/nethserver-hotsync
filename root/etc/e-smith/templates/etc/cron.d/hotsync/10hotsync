{
    if ( $hotsync{role} eq 'server' ) 
    {
        $OUT='0,15,30,45 * * * * root /usr/sbin/hotsync';
    } else {
        $OUT="#HotSync disabled in spare server\n#0,15,30,45 * * * * root /usr/sbin/hotsync";
    }
} 
